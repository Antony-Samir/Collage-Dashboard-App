<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.collagedashboardapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CollageDashboardApplication">
        <activity android:name=".CourseDetails"></activity>
        <activity android:name=".StudentDetails" />
        <activity android:name=".AllStudentsActivity" />
        <activity android:name=".AttendaceActivity" />
        <activity android:name=".AddCoursesActivity" />
        <activity android:name=".CourseActivity" />
        <activity android:name=".AdminActivity" />
        <activity android:name=".TeacherActivity" />
        <activity android:name=".AddStudent" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>